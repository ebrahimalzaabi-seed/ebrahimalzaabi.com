{
  "name": "analytics-proxy",
  "version": "1.0.0",
  "description": "Cloudflare Worker to proxy Microsoft Clarity analytics API",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
